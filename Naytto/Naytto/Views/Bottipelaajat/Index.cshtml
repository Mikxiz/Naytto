@model IEnumerable<Naytto.Models.Bottipelaaja>

@{
    ViewBag.Title = "Index";
}

<h2>Bottipelaaja</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Etunimi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sukunimi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Etunimi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sukunimi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BottaajaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BottaajaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BottaajaID })
        </td>
    </tr>
}

</table>
